<div class="container">
  <div class="card border rounded-4 mt-4">
    <h5 class="card-header text-center">Liste des offres d'emplois</h5>
    <div class="table">
      <table class="table table-hover text-center">
        <thead>
          <tr>
            <th>Entreprise</th>
            <th>Titre de l'annonce</th>
            <th>Description</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% @job_offers.each do |job_offer| %>
            <tr>
              <td><strong><%= job_offer.company.name %></strong></td>
              <td><a href="<%= job_offer_path(job_offer) %>"><%= job_offer.title %></a></td>
              <td><%= job_offer.description %></td>
              <td>
                <div class="btn-group">
                  <button type="button" class="btn btn-primary dropdown-toggle" data-bs-toggle="dropdown">
                    Actions
                  </button>
                  <ul class="dropdown-menu">
                    <li><a class="dropdown-item" href="<%= edit_job_offer_path(job_offer) %>"><i class="bx bx-edit-alt me-1"></i> Modifier</a></li>
                    <li>
                      <%= link_to "Supprimer", job_offer_path(job_offer), data: { turbo_method: :delete, turbo_confirm: "Êtes-vous sûr de vouloir supprimer l'offre?" }, class: "dropdown-item" %>
                    </li>
                  </ul>
                </div>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
